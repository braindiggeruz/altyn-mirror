NEXT_PUBLIC_META_PIXEL_ID=2475663283169925
NEXT_PUBLIC_TELEGRAM_BOT=altyntherapybot
NEXT_PUBLIC_SITE_URL=https://altyn-mirror.pages.dev

# ===== Cloudflare Pages → Settings → Environment variables =====
# Server-only — NEVER expose to the bundle.
#
# Phase 2.3 — ALTYN Mirror analytics ingest (functions/api/mirror-event.ts).
# Default disabled. Flip to 'true' only after the same token is set on the
# backend (.env on the VPS at /srv/altyn/.env: MIRROR_INGEST_TOKEN).
MIRROR_BACKEND_URL=https://bot.altyn-therapy.uz/api/mirror/event
MIRROR_INGEST_TOKEN=
MIRROR_INGEST_ENABLED=false

# Phase 1 — group ping for /api/notify (already in production).
TELEGRAM_BOT_TOKEN=
TELEGRAM_LEADS_CHAT_ID=

# Meta CAPI (functions/api/capi.ts).
META_CAPI_ACCESS_TOKEN=
META_PIXEL_ID=2475663283169925
META_CAPI_TEST_EVENT_CODE=
